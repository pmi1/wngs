{#

Часть шаблона админки для древовидных сущностей: вывод каждой записи

#}

{% for object in admin.datagrid.results %}
    <tr class="treegrid-{{ admin.id(object) }} {% if object.parentId != admin.rootNodeId %}treegrid-parent-{{ object.parentId }}{% endif %}">
        {% include admin.getTemplate('inner_list_row') %}
    </tr>
{% endfor %}
